enum FollowPageType {
  follower,
  following,
}

enum SortType {
  all,
  title,
  registration,
  publication,
}

enum UserInterestType {
  first,
  second,
  third,
}

enum SearchDetailUserInfosType {
  interest,
  readComplete,
  holder,
}
